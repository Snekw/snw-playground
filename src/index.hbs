<!DOCTYPE html>
<html lang="en-US">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{htmlWebpackPlugin.options.title}}</title>
</head>

<body>
    <noscript>
        <div class="error-bar">
            This page relies on JavaScript to show case the "apps" I have made.
        </div>
    </noscript>
    <div id="unsupportedBrowser" class="error-bar hidden">
        <h1>Unsupported browser.</h1>
        <p>Upgrade to a modern browser to see the content correctly.</p>
    </div>
    <header id="frontPageTitle">
        <h1>{{htmlWebpackPlugin.options.app.title}}</h1>
    </header>
    <div id="frontPage" class="container">
        <nav id="appsList" class="container">
            <ul>
                {{#each htmlWebpackPlugin.options.apps}}
                <li>
                    <a href="{{this.outPath}}" data-index="{{this.order}}">{{this.title}}</a>
                </li>
                {{/each}}
            </ul>
        </nav>
        <section id="appContainer" class="container">
            <div>
                <iframe class="appFrame" src="{{htmlWebpackPlugin.options.apps.0.outPath}}"></iframe>
                <section class="appReadmeContainer">
                    <h1></h1>
                    <article></article>
                </section>
            </div>
        </section>
        <template id="appTemplate">
            <div>
                <iframe class="appFrame"></iframe>
                <section class="appReadmeContainer">
                    <h1></h1>
                    <article></article>
                </section>
            </div>
        </template>
    </div>

</body>

</html>
